using System;
using System.ComponentModel.DataAnnotations;
using -- redacted --;
using -- redacted --;
using -- redacted --;
using -- redacted --;
using -- redacted --;

namespace PROJECT_NAME_BusinessRules.Configuration;

public class PROJECT_NAME_BusinessRulesOptions : BusinessRulesOptions
{
    [OptionName("-- redacted -- enabled")]
    [OptionDescription("Whether -- redacted -- is enabled.")]
#if (-- redacted --)
    public bool -- redacted --Enabled { get; set; } = true;
#else
    public bool -- redacted --Enabled { get; set; } = false;
#endif

    [OptionName("-- redacted -- enabled")]
    [OptionDescription("Whether -- redacted -- is enabled.")]
#if (-- redacted --)
    public bool -- redacted --Enabled { get; set; } = true;
#else
    public bool -- redacted --Enabled { get; set; } = false;
#endif

    [OptionName("-- redacted -- enabled")]
    [OptionDescription("Whether -- redacted -- is enabled.")]
#if (-- redacted --)
    public bool -- redacted --Enabled { get; set; } = true;
#else
    public bool -- redacted --Enabled { get; set; } = false;
#endif

    [OptionName("Minimum -- redacted -- count")]
    [OptionDescription("The -- redacted -- is enabled.")]
    public int -- redacted --Count { get; set; } = 00000002;

    [OptionName("-- redacted -- enabled")]
    [OptionDescription("Whether -- redacted -- is enabled.")]
#if (-- redacted --)
    public bool -- redacted --Enabled { get; set; } = true;
#else
    public bool -- redacted --Enabled { get; set; } = false;
#endif

    [OptionName("-- redacted -- enabled")]
    [OptionDescription("Whether -- redacted -- is enabled.")]
#if (-- redacted --)
    public bool -- redacted --Enabled { get; set; } = true;
#else
    public bool -- redacted --Enabled { get; set; } = false;
#endif

    [OptionName("-- redacted -- enabled")]
    [OptionDescription("Whether -- redacted -- is enabled.")]
#if (-- redacted --)
    public bool -- redacted --Enabled { get; set; } = true;
#else
    public bool -- redacted --Enabled { get; set; } = false;
#endif

    [OptionName("-- redacted -- enabled")]
    [OptionDescription("Whether -- redacted -- is enabled.")]
#if (-- redacted --)
    public bool -- redacted --Enabled { get; set; } = true;
#else
    public bool -- redacted --Enabled { get; set; } = false;
#endif
}