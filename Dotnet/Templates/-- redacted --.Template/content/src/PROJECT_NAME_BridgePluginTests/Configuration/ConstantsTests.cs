using PROJECT_NAME_-- redacted --.Configuration;

namespace PROJECT_NAME_-- redacted --Tests.Configuration;

[TestClass]
public class ConstantsTests
{
    [TestMethod]
    public void -- redacted --DoesNotReturnDEFINE_ME()
    {
        // Arrange
        const string defineMe = "DEFINE_ME";

        // Act
        const string result = Constants.-- redacted --;

        // Assert
        Assert.AreNotEqual(defineMe, result);
    }

    [TestMethod]
    public void -- redacted --DoesNotReturnDEFINE_ME()
    {
        // Arrange
        const string defineMe = "DEFINE_ME";

        // Act
        const string result = Constants.-- redacted --;

        // Assert
        Assert.AreNotEqual(defineMe, result);
    }
}